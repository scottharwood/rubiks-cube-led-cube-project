class Node:
    def _init_(self, start, stop):
        self.x = coordinates[0]
        self.y = coordinates[1]
        self.y = coordinates[2]

class Edge:
    def _init_(self):
        self.start = start
        self.stop = stop

class WireFrame:
    def _init_(self):
        self.node = []
        self.edges = []

def addNodes(self.nodeList):
    for node in nodeList:
        self.nodes.append(Node(node))

def addEdges(self, edgeList):
    for (start, stop) in egdeList:
        self.edges.appened(Edge(self.nodes[start], self.nodes[stop]))

my_wireframe = wireframe()
my_wireframe.addNodes([(0,0,0), (1,2,3), (3,2,1)])
my_wireframe.addNodes([(1,2)])

        
